package chocostock.auxiliar;

import chocostock.interfaces.ValidadorInput;

public class Escolhe implements ValidadorInput {
//    public static LocalDate dataFutura(Scanner scanner, String prompt, String mensagemErro) { // colocar para outro lugar, pq aqui n faz sentido sendo q usa essa funcao até no ingrediente
//        return LocalDate.parse((getInput(scanner, prompt, mensagemErro, Verifica::isDataFutura)), DateTimeFormatter.ofPattern("dd/MM/yyyy"));
//    }
//
//    public static LocalDate data(Scanner scanner, String prompt, String mensagemErro) {
//        return LocalDate.parse((getInput(scanner, prompt, mensagemErro, Verifica::isData)), DateTimeFormatter.ofPattern("dd/MM/yyyy"));
//    }
}
